<Window x:Class="Uzi.Ikosa.Workshop.ModuleManagement.NewCampaignDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Uzi.Ikosa.Workshop.ModuleManagement"
        mc:Ignorable="d" 
        ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterOwner"
        Title="Create New Campaign" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="2,1"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="MinWidth" Value="56"/>
        </Style>
    </Window.Resources>
    <DockPanel  FocusManager.FocusedElement="{Binding ElementName=txtCampaignName}">
        <DockPanel DockPanel.Dock="Bottom" LastChildFill="False" Margin="4">
            <Button IsCancel="True" DockPanel.Dock="Right" Content="Cancel" Click="btnCancel_Click" TabIndex="1" />
            <Button IsDefault="True" DockPanel.Dock="Right" Tag="Portal" Content="OK" Command="{Binding Path=OKCommand}" TabIndex="2" />
        </DockPanel>
        <Label Content="Campaign Name" DockPanel.Dock="Top"></Label>
        <TextBox DockPanel.Dock="Top" Margin="2" x:Name="txtCampaignName" TabIndex="0" ></TextBox>
        <Label Content="Existing" DockPanel.Dock="Top"></Label>
        <ItemsControl ItemsSource="{Binding Path=Campaigns}" Margin="2" x:Name="lstPackageSets" 
                      DisplayMemberPath="Name"
                      BorderBrush="DarkGray" BorderThickness="1" VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch">
        </ItemsControl>
    </DockPanel>
</Window>
